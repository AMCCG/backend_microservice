FROM openjdk:21-jdk
VOLUME /tmp
ARG PORT
ARG EUREKA_SERVER_DEFAULT_ZONE
ENV SERVER_PORT=$PORT
ENV EUREKA_SERVER_DEFAULT_ZONE=$EUREKA_SERVER_DEFAULT_ZONE
COPY target/*.jar app.jar
ENTRYPOINT ["java","-jar","/app.jar"]